apiVersion: v1
kind: Service
metadata:
  labels:
    pulp.service: backend
  name: backend
spec:
  ports:
  - name: "8000"
    port: 8000
    targetPort: 8000
  selector:
    pulp.service: backend
status:
  loadBalancer: {}
